# Tool Preferences

- Use the `searxng` tool for all web searches. It uses a local SearXNG instance.
- If `searxng` reports it's not available (off home network), fall back to `webfetch` with a search engine URL (e.g., `https://duckduckgo.com/?q=...`).
